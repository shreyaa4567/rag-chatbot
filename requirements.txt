# Direct dependencies only — pip resolves the transitive tree.
# Pinned to known-good versions. Regenerate a full lockfile with `pip freeze`
# if you need byte-for-byte reproducibility.

# Web API
Flask==3.1.3
flask-cors==6.0.5
waitress==3.0.2          # production WSGI server (Windows-friendly)

# Crawling
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1              # parser used by BeautifulSoup
tldextract==5.3.1        # registered-domain matching for same-site crawl

# RAG / vector store / LLM
chromadb==1.5.9
ollama==0.6.2            # local LLM + embeddings client
langchain-text-splitters==1.1.2   # RecursiveCharacterTextSplitter

# Config
python-dotenv==1.2.2

# Testing (dev)
pytest==9.1.1
